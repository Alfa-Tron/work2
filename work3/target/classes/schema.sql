CREATE TABLE IF NOT EXISTS your_table_name (
                                               id INTEGER PRIMARY KEY AUTOINCREMENT,
                                               field1 TEXT,
                                               field2 INTEGER,
    -- Добавьте остальные поля из вашего JSON-файла
);